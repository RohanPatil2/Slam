# SLAM Toolbox Configuration for Uncertainty-Aware SLAM
# Optimized for 20m×20m LARGE SIMPLE environment with REAL-TIME mapping

slam_toolbox:
  ros__parameters:

    # General Parameters
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # ROS Parameters
    odom_frame: odom
    map_frame: map
    base_frame: base_footprint
    scan_topic: /scan
    use_map_saver: true
    mode: mapping

    # RESOLUTION: 0.05m (5cm) for HIGH QUALITY mapping
    # For 10m × 10m: 200×200 = 40,000 cells (good balance)
    resolution: 0.05

    # MAP UPDATE: VERY FAST updates for immediate visibility
    map_update_interval: 0.3  # Update every 0.3 seconds (3.3 Hz)
    map_start_pose: [0.0, 0.0, 0.0]

    # SMOOTH matching for quality mapping with slower robot
    minimum_travel_distance: 0.2  # Process scans less frequently for quality (was 0.1)
    minimum_travel_heading: 0.15   # Process rotation updates conservatively (was 0.1)
    scan_buffer_size: 50  # Larger buffer for better matching (was 40)
    scan_buffer_maximum_scan_distance: 4.5  # Match sensor max range (4.0m + margin)
    link_match_minimum_response_fine: 0.5  # More strict for quality (was 0.3)
    link_scan_maximum_distance: 10.0  # Match environment size (10m)
    
    # Loop closure - tuned for 20m environment
    loop_search_maximum_distance: 8.0  # Larger for 20m environment
    do_loop_closing: true
    loop_match_minimum_chain_size: 8  # Less strict (was 10)
    loop_match_maximum_variance_coarse: 3.5  # More permissive (was 3.0)
    loop_match_minimum_response_coarse: 0.3  # Less strict (was 0.35)
    loop_match_minimum_response_fine: 0.35  # Less strict (was 0.45)

    # Correlation Parameters - Faster search
    correlation_search_space_dimension: 0.3  # Smaller search (was 0.5)
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # Loop Closure Parameters - Faster
    loop_search_space_dimension: 6.0  # Smaller search (was 8.0)
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # Scan Matcher Parameters - Quality focused
    distance_variance_penalty: 0.5  # Standard penalty for quality (restored)
    angle_variance_penalty: 1.0  # Standard penalty (restored)
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9  # More strict for quality (restored)
    minimum_distance_penalty: 0.5  # More strict for quality (restored)
    use_response_expansion: true
    
    # Additional parameters for quality mapping with degraded sensor
    max_laser_range: 4.2  # Match degraded sensor (4.0m + small margin)
    minimum_time_interval: 0.5  # Slower scan processing for quality (was 0.3)
    transform_publish_period: 0.05  # 20Hz TF publishing (was 0.02)
    tf_buffer_duration: 30.0  # Keep TF history
    
    # Map quality parameters
    minimum_map_update_frequency: 3.0  # Ensure map updates at least 3 Hz
    stack_size_to_use: 40000000  # Increase stack size for processing
